
public class Node {
		/* The Node class for storing objects that are linked together. */

		Object data;
		Node next;

		Node (Object d, Node n) {
		  this.data = d;
		  this.next = n;
		}
}
